<UserControl x:Class="WatcHive.View.ListasUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WatcHive.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="  Vistos " Grid.Row="0" Foreground="White" FontFamily="Montserrat" FontWeight="Bold">
            <Border CornerRadius="5" BorderThickness="0" ClipToBounds="True" BorderBrush="White" Background="White" Margin="2">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <WrapPanel x:Name="panelVistos" />
                </ScrollViewer>
            </Border>
        </GroupBox>

        <GroupBox Header="  Pendientes " Grid.Row="1" Foreground="White" FontFamily="Montserrat" FontWeight="Bold">
            <Border CornerRadius="5" BorderThickness="0" ClipToBounds="True" BorderBrush="White" Background="White" Margin="2">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <WrapPanel x:Name="panelPendientes" />
                </ScrollViewer>
            </Border>
        </GroupBox>
    </Grid>
</UserControl>
